## 🚀 트러블슈팅 및 오답 노트 (Highlight)

### [프로그래머스 - 모음사전 (Python / 완전탐색)](./Programmer/Level_Tests/Level_2/Level_2_모음사전.py)
* **문제 상황:** 5글자 이하의 모음 조합 단어 위치를 찾는 과정에서 자릿수가 변할 때 사전의 정렬 순서가 넘어가는 메커니즘이 직관적으로 이해되지 않아 로직 설계에 혼선이 발생함
* **문제 원인:** 컴퓨터는 문자열을 정렬할 때 전체 길이가 아닌 맨 앞 자릿수부터 한 글자씩 아스키코드 순서로 비교하는 사전식 순서(Lexicographical Order)를 따름. 이로 인해 AAAAE가 글자 수가 더 적은 AAAE보다 앞 순위에 배치되는 문자열 정렬 특성을 간과함
* **해결 방안:** 다중 for문 대신 itertools 패키지의 product(중복순열) 라이브러리를 활용해 모든 튜플 조합을 추출함. join 함수로 문자열 단어로 변환한 뒤 파이썬의 sort 함수로 정렬하여 완벽한 모음사전을 빌드함. 이후 enumerate 함수로 선형 탐색하여 단어와 일치하는 인덱스에 1을 더한 값을 반환해 해결함

### [프로그래머스 - 방문 길이 (Python / 구현)](./Programmer/Level_Tests/Level_2/Level_2_방문길이.py)
* **문제 상황:** 반복문을 순회하며 visited 2차원 배열에 정점 좌표를 기록하고 answer를 증가시키는 코드 결과에서 오답 및 인덱스 에러(IndexError)가 발생함
* **문제 원인:** 경계값 검증 분기가 visited 배열 조회보다 뒤에 있어 격자 밖을 벗어나는 순간 범위를 초과함. 또한 문제의 본질인 걸어본 길(간선)이 아닌 점(정점)을 방문 처리하여 다른 경로로 진입하는 새로운 길까지 중복 처리되는 논리적 오류와 양방향 이동에 대한 예외 처리가 누락됨
* **해결 방안:** 2차원 visited 배열 대신 지나간 선의 주소를 중복 없이 담아내기 위해 set 자료구조를 도입함. 좌표 경계 검증을 최우선 분기로 배치해 에러를 차단하고, 이동 시 정방향/역방향 간선 쌍을 생성하여 최초 방문일 때만 set에 추가함. 최종적으로 set의 총 길이에 2를 나눈 몫을 반환하여 해결함

### [프로그래머스 - n^2 롤케이크자르기 (Python / 구현)](./Programmer/Level_Tests/Level_2/Level_2_롤케이크자르기.py)
* **문제 상황:** 반복문을 순회하며 set을 이용해 중복없이 갯수를 새는 코드 결과에서 시간 초과가 발생함
* **문제 원인:** 반복문을 순회하며 set의 탐색/삭제 시 시간복잡도 O(1)을 이용하여야 하는데 순회하며 새로운 set을 생성하여 오히려 시간복잡도 O(n)을 사용해버리는 문제가 발생함
* **해결 방안:** set이 아닌 딕셔너리를 사용하여 동생의 바구니를 먼저 꽉 채우고 반복문 순회하며 동생의 바구니에서 뺏어 형의 바구니에 담아주는 로직으로 문제 해결

### [프로그래머스 - n^2 배열 자르기 (Python / 구현)](./Programmer/Level_Tests/Level_2/Level_2_n^2배열자르기.py)
* **문제 상황:** 문제에 제시된 조건 그대로 $n \times n$ 크기의 2차원 배열을 중첩 `for`문으로 직접 생성한 뒤, `max(행, 열) + 1` 규칙으로 값을 채우고 `[left:right+1]`로 슬라이싱하여 반환하도록 구현했으나 파이썬 환경에서 `시간 초과(Time Out)` 및 `메모리 초과(Memory Limit Exceeded)` 발생함
* **문제 원인:** 입력값 $n$의 크기가 최대 10,000,000(1,000만)에 육박함. 이 상태에서 실제 2차원 배열 리스트를 물리적으로 가동하면 데이터 개수가 $n^2$인 **100조 개**가 되어 버림. 이는 $O(N^2)$의 무거운 시간 복잡도를 유발해 연산이 끝나지 않을 뿐만 아니라, 수백 GB의 메모리가 필요하므로 파이썬의 리스트 할당 한계를 초과하여 무조건 터질 수밖에 없는 구조적 원인이었음
* **해결 방안:** - 100조 개짜리 대형 리스트를 생성하는 방식을 완전히 배제하고, 필요한 구간(`left` ~ `right`)만 정밀 타격하는 **가상 행렬 좌표 변환 기법**을 도입함
  - 구하고자 하는 구간의 길이(`right - left`)는 최대 100,000(10만)밖에 안 된다는 점에 착안하여, 해당 범위의 1차원 인덱스 $k$를 $n$으로 나눈 몫과 나머지 연산인 `row = k // n`과 `col = k % n`을 통해 2차원 좌표를 $O(1)$ 만에 실시간으로 역산함
  - 도출된 좌표에 파이썬 내장 함수인 `max(row, col) + 1` 수학적 규칙을 다이렉트로 적용하여 값을 채움으로써, 100조 번 돌던 연산을 단 10만 번의 상수 기반 선형 탐색($O(N)$)으로 압축하여 효율성 테스트를 완벽하게 통과함

### [프로그래머스 - 귤고르기 (구현)](./Programmers/Level_Tests/Level_2/Level_2_귤고르기.py)
* **문제 상황:** 조합(Combination) 구조에 매몰되어 완전 탐색(DFS/백트래킹)으로 가지치기를 하며 풀려고 시도했으나, 구현이 극도로 복잡해지고 테케 제출 시 시간 및 메모리 초과 발생함
* **문제 원인:** 귤의 총개수가 최대 10만 개, 종류가 최대 1000만 개에 육박하는 대규모 데이터셋 구조임. 이런 상황에서 DFS를 적용하면 상태 트리의 분기가 기하급수적으로 늘어나 $O(2^N)$의 지수 시간 복잡도를 가지게 되므로, 파이썬 재귀 한도 초과 및 시간 초과가 발생할 수밖에 없는 환경이었음
* **해결 방안:** - "종류의 최소화"를 달성하기 위해 매 순간 개수가 가장 많은 귤 종류부터 먼저 쓸어 담는 **그리디(탐욕법)** 조건이 성립함을 파악함

### [프로그래머스 - 짝지어제거하기 (스택)](./Programmers/Level_Tests/Level_2/Level_2_짝지어제거하기.py)
* **문제 상황:** 문자열을 리스트로 변환하고 연속하는 알파벳이 존재 시, remove를 이용하여 제거하고 다시 문자열을 순회할 때 시간초과 발생함
* **문제 원인:** remove 함수를 이용하면 리스트를 다시 탐색해야하기 때문에 총 시간복잡도가 O(n^2)이라는 시간이 걸려 시간초과가 발생함
* **해결 방안:** 스택을 이용하여 문자열이 연속하지않다면 스택에 집어넣고 연속하면 스택의 top에 있는 요소를 pop하여 시간복잡도를 해결함

### [프로그래머스 - 다음 큰 숫자 (구현)](./Programmers/Level_Tests/Level_2/Level_2_다음큰숫자.py)
* **문제 상황:** n보다 큰 숫자를 1씩 늘려가며 2진수의 '1' 개수를 비교하는 로직을 구현했으나, 효율성 테스트에서 무조건 시간 초과 발생함
* **문제 원인:** 숫자가 커질 때마다 2진법 변환 함수(binary)를 직접 호출해서 문자열을 만드는 오버헤드가 누적됨
* **해결 방안:** 루프 내부의 무거운 객체 생성 로직을 전부 걷어내고, 파이썬 내장 함수인 bin()과 .count() 조합으로 선회함

### [프로그래머스 - 조이스틱 (구현)](./Programmers/Level_Tests/Level_2/Level_2_조이스틱.py)
* **문제 상황:** 알파벳을 변경하는 상하 움직임과 커서를 이동하는 좌우 움직임을 동시에 처리하려다보니 코드가 터지고 구현이 어려움
* **문제 원인:** 매 커서 위치마다 좌,우 어디가 최선인지를 판단하는 그리디 방식을 따르다 보니 코드가 조건을 제대로 따라가지 못하고 터지는 상황이 발생함
* **해결 방안:** 좌,우 로직과 상,하 로직을 분리하여 코드를 구현함

### [프로그래머스 - 구명보트 (구현)](./Programmers/Level_Tests/Level_2/Level_2_구명보트.py)
* **문제 상황:** 이중 루프나 일반 리스트 기반의 큐(Queue) 연산을 사용해 시뮬레이션을 구현했을 때 효율성 테스트에서 시간 초과 발생.
* **문제 원인:** - 문제의 최대 데이터 수($N$)가 50,000개이므로, 이중 루프를 통한 완전 탐색 시 $O(N^2)$의 시간 복잡도를 가짐(약 25억 번 연산).
  - 파이썬 리스트의 pop(0)을 활용해 큐 방식으로 구현할 경우, 원소 탈출 후 배열 재정렬 비용O(N)이 매 루프마다 발생하여 최종적으로 $O(N^2)$의 비효율성을 유발함.
* **해결 방안:** - 탐욕법(Greedy) 구조에 착각하여 데이터를 정렬O(N \log N)한 뒤, 양방향에서 경계를 좁혀오는 **투 포인터(Two Pointer)**  알고리즘으로 선회함.
  - 리스트 자체를 수정하거나 요소를 삭제하지 않고, left와 right 인덱스 포인터만 이동시키며 한 번의 순회 O(n)로 모든 매칭을 끝냄으로써 대규모 데이터셋에서도 시간 초과 없이 효율성 테스트를 통과함.
  
### [프로그래머스 - [3차]파일명정렬 (구현)](./Programmers/Level_Tests/Level_2/Level_2_[3차]파일명정렬.py)
* **문제 상황:** 파일명을 head,number,tail 세 부분으로 분리하기 위해 문자열 내에서 숫자가 시작하는 부분과 끝나는 부분을 각각 min(start,i), max(end,i) 함수로 관리 시, 파일명이 규칙대로 쪼개지지 않는 문제 발생
* **문제 원인:** number 뒤에 숫자가 올지라도 제대로 처리하지 못하고 end에 바로 처리됌
* **해결 방안:** start, end 변수 생성 시 -1을 넣어 초기값이 -1과 같다면 start 인덱스 갱신, end 변수는 number가 5글자 이상, 또는 숫자로 끝날 때 끝 인덱스를 넣어 오류 해결

### [프로그래머스 - 행렬테두리회전하기 (구현)](./Programmers/Level_Tests/Level_2/Level_2_행렬테두리회전하기.py)
* **문제 상황:** 주어지는 회전 범위가 달라지므로, 전체 행렬에서 해당 범위만큼 rep_M으로 부분행렬을 떼어낸다
* **문제 원인:** 원본 행렬에서 (x1, y1) ~ (x2, y2) 범위를 잘라내어 새 배열(rep_M)을 만드는 순간, 인덱스가 전부 0번지 기준으로 압축됨.
* **해결 방안:** 범위를 잘라내어 복사하는 방식은 리스크도 크고 어려워 원본 행렬 위에서 움직이는 구현 방식으로 코드를 재현함

### [프로그래머스 - 실패율 (구현)](./Programmers/Level_Tests/Level_1/Level_1_실패율.py)
* **문제 상황:** 특정 스테이지에 도달한 플레이어 수가 0명인 상태 및 스테이지별 인원 카운트 시 연산 낭비 상황 발생
* **문제 원인:** 매번 리스트 탐색 시 O(n)의 시간복잡도를 발생하여 시간 초과 발생, 스테이지가 올라갈수록 인원수가 줄어들어 0명이 되어 ZeroDivisionError 가 발생
* **해결 방안:** collections.Counter 함수를 이용하여 스테이지를 해시로 생성하면 탐색 시에도 O(1)의 시간복잡도를 가져 시간초과 해결, 예외 처리를 통해 분모가 0이 되면 실패율에 0을 바로 대입하여 오류 해결

### [프로그래머스 - 키패드 누르기 (구현)](./Programmers/Level_Tests/Level_1/Level_1_키패드누르기.py)
* **문제 상황:** (2,5,8,0) 입력 버튼 시, 거리 계산이 잘못되는 경우 발생
* **문제 원인:** 눌러야 할 버튼의 왼손과 오른손 거리를 비교하고 난 후 왼(오른)손 위치 초기화(갱신)을 해주지 않음
* **해결 방안:** 거리 비교하는 조건문 내부에 answer 문자열을 갱신한 다음 코드에 L_loc/R_loc 위치 변수 갱신

### [프로그래머스 - 카펫 (구현)](./Programmers/Level_Tests/Level_2/Level_2_카펫.py)
* **문제 상황:** 상황: 프로그래머스 '카펫' 문제 풀이 중, 전체 타일 개수(brown + yellow)의 약수 쌍 (가로, 세로)을 구했음에도 특정 테스트 케이스에서 오답이 발생함.
* **문제 원인:** 전체 면적의 올바른 약수 쌍이라 할지라도, 문제에서 요구한 테두리와 알멩이 부분의 격자 갯수가 맞지 않는 가짜 약수 쌍이 나올 수 있음.
* **해결 방안:** 노란색 격자 크기가 (가로 - 2) * (세로 - 2) 의 갯수를 만족하는 조건이 추가되어야 함

### [프로그래머스 - 완주하지 못한 자 (해시)](./Programmers/Level_Tests/Level_1/Level_1_완주하지못한자.py)
* **문제 상황:** 참가자 배열을 순회하며 완주자 배열을 순회하는 이중 중첩문 사용 시 O(n^2)의 시간복잡도가 발생하여 참가자가 100,000이라면 시간초과 발생
* **해결 방안:** 해시 테이블을 생성하여 삽입, 탐색 연산 시에도 시간복잡도를 줄여야 효율성 테스트 통과가 가능함

### [프로그래머스 - 의상 (해시)](./Programmers/Level_Tests/Level_2/Level_2_의상.py)
* **문제 상황:** 의상의 개수가 늘어날 때마다 모든 조합을 완전 탐색하려면 시간초과 O(n^2)만큼의 시간복잡도가 발생
* **해결 방안:** 해시 테이블을 생성하여 삽입, 탐색 연산 시에도 시간복잡도를 줄여야 효율성 테스트 통과가 가능함

### [프로그래머스 - 올바른 괄호 (스택)](./Programmers/Level_Tests/Level_2/Level_2_올바른괄호.py)
* **문제 상황:** 문자열의 최대 길이가 100,000이므로, 만약 이중 반복문을 사용할 경우 O(n^2)의 시간복잡도가 발생하여 코드가 터지는 상황 발생
* **해결 방안:** 이중 반복문을 사용하는 것이 아닌 자료구조 중 스택을 사용하여, 문자열을 한 번만 순회하여 괄호 쌍이 맞지 않는다면 False를 return, 쌍이 맞다면 True를 return 최적화를 함

### [프로그래머스 - 가장 큰 수 (정렬)](./Programmers/Level_Tests/Level_2/Level_2_가장큰수.py)
* **문제 상황:** 자릿수 다를 때 사전순 정렬 오류
* **해결 방안:** `x * 3` 정렬 기법 및 예외 처리

### [백준 5430 - 쇠막대기 (스택)](./Beakjoon/Data_Structure/Stack/10799_쇠막대기.py)
* **문제 상황:** 조건문 복잡화로 시간 초과 발생
* **해결 방안:** 스택 크기(`len(stack)`)를 활용한 분기 최적화로 $O(N)$ 해결

